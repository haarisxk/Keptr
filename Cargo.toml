[workspace]
members = [
    "keptr-crypto",
    "keptr-core",
    "keptr-store",
    "src-tauri"
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort" # Prevents unwinding on panics to avoid leaking secrets
strip = true
